#include <bits/stdc++.h>
using namespace std;
#define PB push_back
#define ll long long
const int INF = 0x3f3f3f3f;
void solve()
{
    int n,m;
    cin >> n >> m;
    vector<ll> a,b,c;
    ll aux;
    for(int i=0;i<n;i++)
    {
        cin >> aux;
        a.push_back(aux);
    }
    for(int j=0;j<m;j++)
    {
        cin >> aux;
        b.push_back(aux);
    }
    c = b;
    so
}
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t=1;
    cin >> t;
    while(t--) solve();
}