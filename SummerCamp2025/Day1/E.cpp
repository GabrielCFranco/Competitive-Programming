#include <bits/stdc++.h>
using namespace std;
#define PB push_back
#define ll long long
#define MAX_V 10000
int main()
{
    int n;
    cin >> n;
    vector<int> v;
    for(int i=0;i<n;i++)
    {
        int aux;
        cin >> aux;
        v.PB(aux);
    }
    vector<int> vaux;
    vaux = v;
    sort(vaux.begin(),vaux.end());
    int 
}